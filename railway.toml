[build]
builder = "nixpacks"
buildCommand = "rm -rf dist && ./node_modules/.bin/tsc"
installCommand = "npm ci"
nixpacks = { version = "node20" }
nodeVersion = "20"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartDelay = 5
healthcheckInterval = 15
healthcheckStartPeriod = 30
healthcheckSuccessThreshold = 1
healthcheckFailureThreshold = 3

[build.environment]
NODE_ENV = "production"
NODE_OPTIONS = "--max_old_space_size=1024"
PORT = "3000"

[deploy.regions]
primary = "europe-west1"
fallback = "us-east1"

[deploy.environment]
NODE_ENV = "production"
PORT = "3000"
TRUST_PROXY = "true"
EXPRESS_DISABLE_RATE_LIMIT = "true"

# Email beállítások
USE_EMAIL = "advanced"  # 'advanced' mód bekapcsolva

# Email URL-ek
VERIFY_EMAIL_URL = "https://need-shit.fun/verify"
PASSWORD_RESET_URL = "https://need-shit.fun/password-reset"
CHANGE_EMAIL_URL = "https://need-shit.fun/verify-email-address-change"

# SMTP beállítások a Docker SMTP szerverhez
EMAIL_SMTP_HOST = "smtp"
EMAIL_SMTP_PORT = "587"
EMAIL_SMTP_USER = "noreply@need-shit.fun"
EMAIL_SMTP_PASS = "erős_jelszó_ide"
EMAIL_SMTP_SECURE = "false"
EMAIL_FROM_ADDRESS = "\"Vendure Store\" <noreply@need-shit.fun>"

# SMTP szerver beállítások
SMTP_HOST_PORT = "25"
SMTP_HOST_PORT_SUBMISSION = "587"